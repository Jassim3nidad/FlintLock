<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Flintlock itself never makes a network call — no backend, no
      account, no telemetry. This permission is staged for the approved
      but not-yet-implemented Web Bridge local-transfer feature (see
      docs/WEB_BRIDGE_THREAT_MODEL.md): Android has no permission scoped
      to "local network only," so any local socket needs the same
      broad INTERNET permission a WAN request would. Enforcing
      local-only stays a code-review guarantee, not an OS one — see
      SECURITY.md.
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>

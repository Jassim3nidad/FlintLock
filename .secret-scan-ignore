# Paths excluded from scripts/security/scan-secrets.js.
# Every entry here needs a reason — this file is a liability if it grows
# without scrutiny.

# The RFC 4648 base32 alphabet constant — a public, fixed 32-character
# string, not a secret. Flagged purely for entropy (5 bits/char, all
# distinct characters), independent of any signature match.
packages/core/src/totp/base32.ts

# TOTP/HOTP tests use 'JBSWY3DPEHPK3PXP' (base32 for "Hello!\xde\xad\xbe\xef",
# the standard example secret from RFC 6238-adjacent tutorials and the
# otpauth spec's own examples) and similar placeholder base32 strings as
# fixture data. These are well-known public test values, not live secrets.
# (Paths below moved under the monorepo split — see docs/CRYPTO.md.)
packages/core/src/totp/__tests__/otpauth.test.ts
packages/core/src/vault/__tests__/totpService.test.ts
packages/core/src/vault/__tests__/securityDashboard.test.ts
apps/mobile/src/components/__tests__/TotpCodeDisplay.test.tsx
apps/mobile/src/screens/__tests__/CredentialDetailScreen.test.tsx
apps/mobile/src/screens/__tests__/SecurityDashboardScreen.test.tsx

# Same well-known TOTP example secret, plus the checked-in golden .flbx
# fixture itself: base64-encoded AES-256-GCM ciphertext (salt + IV + tag
# + encrypted payload), which is *supposed* to be high-entropy — that's
# what encryption is for. It's encrypted under a fixed, deliberately
# public test password ('correct horse battery staple', declared in the
# same file) and a fake password history entry named
# 'old-password-example'. See goldenFlbxCheck.ts: if this fixture ever
# needs regenerating, that's a reviewed, deliberate action, not a leak.
packages/core/src/testing/fixtures/goldenFlbx.ts
